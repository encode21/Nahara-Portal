/**
 * Dashboard re-export — implementation lives in components/situation.
 * Preserves existing import path used by the portal dashboard.
 */
export { SituationCenter } from "@/components/situation/SituationCenter";
